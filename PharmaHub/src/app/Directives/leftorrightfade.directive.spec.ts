import { LeftorrightfadeDirective } from './leftorrightfade.directive';

describe('LeftorrightfadeDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftorrightfadeDirective();
    expect(directive).toBeTruthy();
  });
});
