import { MoveUpAnimateDirective } from './move-up-animate.directive';

describe('MoveUpAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveUpAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
